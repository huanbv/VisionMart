# Shared Kernel

Cross-module **domain primitives**: value objects, common identifiers, money
types, pagination, base aggregate root, and other concepts that legitimately
belong to multiple bounded contexts.

> Only put something here if removing it would force duplication across two or
> more modules. When in doubt, keep it inside the module.
